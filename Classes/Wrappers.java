package Classes;

public class Wrappers {
    public static void main(String[] args) { 
    // Goes from primitive to wrapper class.
    Boolean outThere = true;
    Character asterisk = '*'; 
    Integer minteger = 6734; 
    Double pieYum = 3.14;
    String job = "President"; 

    // Reference data types, wrapper classes have some better methods. 
    // Boolean name. (Gives methods).

    if(outThere == true && pieYum == 3.14 && asterisk == '*' && minteger == 6734 && job == "President") { 
        System.out.println("This is so out there");
    }
}
} 
